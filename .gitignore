# dependencies
node_modules/

# build output / packaged extension
dist/
*.zip

# secrets — OAuth keys, signing keys
*.pem
key.pem
.env
.env.*

# os
.DS_Store

# saved-page vendor assets — only the .html fixture is used (by jsdom in tests)
data/fixtures/*_files/
